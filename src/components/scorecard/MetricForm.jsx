import React from 'react';

const MetricForm = ({ initialData = {}, onClose }) => {
  return (
    <div>
      <p>MetricForm placeholder</p>
      <button onClick={onClose}>Close</button>
    </div>
  );
};

export default MetricForm;